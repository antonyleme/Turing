import styled from 'styled-components'

const Title = styled.h2`
  font-size: 48px;
  font-weight: 600;
  line-height: 64px;
  color: black;

  @media (max-width: 1024px) {
    font-size: 30px;
    line-height: 40px;
  }
`

export default Title